# job_hunting

Chrome extension that auto-fills and submits job applications across ATS sites (Greenhouse, Lever, Workday, etc.). Personal data is never committed — it lives in chrome.storage and gitignored `*.local` files.
